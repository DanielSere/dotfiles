#!/bin/bash

((nmcli --fields "SECURITY,SSID" device wifi list | sed 1d | sed 's/  */ /g' | sed -E "s/WPA*.?\S/ /g" | sed "s/^--/ /g" | sed "s/  //g" | sed "/--/d")& rofi -dmenu -p 'Getting list of available Wi-Fi networks...' )
